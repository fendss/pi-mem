# PiMem Architecture

PiMem is a domain-oriented modular monolith. Top-level modules describe product capabilities; ports and adapters isolate external technologies inside each capability.

## Contexts

| Context | Owns | Must not own |
|---|---|---|
| `memory` | Immutable source memories, scopes, sessions, ingest, exact reads | Ranking, citations, benchmark answers |
| `retrieval` | Search requests, hits, ranking, embeddings, temporal and numeric facts | Agent protocol, citations, benchmark gold |
| `evidence-agent` | Search/read/finish protocol, ledger, evidence selection, citations | Benchmark answer formatting and evaluation |
| `benchmark` | Questions, answer stage, durable runs, retry, artifacts, evaluation | Raw-memory or retrieval implementation details |

`entrypoints` parses external input. `composition` is the only module that wires concrete adapters together. `platform` contains technology adapters shared by more than one context and must not contain domain policy.

## Navigation

- The generated [file catalog](file-catalog.md) lists every source file, layer, status, and responsibility.
- The generated [code catalog](code-catalog.md) lists every top-level function, class, and class method with its purpose and source line.
- [Dependency rules](dependency-rules.md) define allowed imports.
- [Glossary](glossary.md) defines the terms used across context boundaries.
- [Architecture decisions](decisions/) record the rationale and consequences of important choices.

Both catalogs are generated by `npm run docs:catalog` and checked for freshness by `npm run docs:catalog:check`.

## Source Map

```text
src/
|-- memory/                 immutable source-memory models, ingest use case, and store port
|-- retrieval/              search orchestration, ranking, operators, embedding model, and adapters
|-- evidence-agent/         evidence ledger, Pi tools, prompts, and bounded agent runtime
|-- benchmark/              benchmark models, LongMemEval adapter, answering, and question use case
|-- entrypoints/cli/        argument parsing, command handlers, and the CLI router
|-- composition/            construction of concrete retrieval profiles
|-- platform/               SQLite, Pi SDK, filesystem, security, and concurrency adapters
|-- util.ts                 small technology-neutral shared helpers
`-- *.ts                    temporary compatibility facades for pre-refactor import paths
```

## Dependency Direction

```text
entrypoints -> composition
benchmark   -> evidence-agent -> retrieval -> memory
                         \-----------------> memory

adapters -> ports/use-cases/model in the same context
composition -> concrete adapters
```

Dependencies point toward stable policy. Database rows, Pi SDK messages, OpenAI payloads, Docker arguments, and filesystem layouts stay in adapters.

The structure applies Parnas-style information hiding at file boundaries, DDD bounded contexts at capability boundaries, and ports-and-adapters for technology boundaries. Characterization tests and compatibility facades implement branch-by-abstraction while observable behavior remains frozen.

## File Rule

A file owns one reason to change, not necessarily one function. Functions stay together when they implement the same policy and have the same dependencies and test strategy. Split a file when its contents change for different reasons or require different external technologies.
